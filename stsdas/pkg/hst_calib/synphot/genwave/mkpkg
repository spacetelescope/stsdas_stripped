# Update the genwave application code in the synphot package library
# Author: MEDEIROS 23-JAN 1989

$checkout synphot.a ../
$update   synphot.a
$checkin  synphot.a ../
$exit

synphot.a:
	genwave.x
	x_genwave.x	<tbset.h>
	;
